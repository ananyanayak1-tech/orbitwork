{
  "buildCommand": "cd frontend && CI=false GENERATE_SOURCE_MAP=false react-scripts build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
